/* ==========================================================
   config.js — 這裡填入你的金鑰
   ----------------------------------------------------------
   這個檔案不會進 git（見 .gitignore），只存在你自己的電腦/repo。
   照著「申請帳號步驟清單」拿到的值，一個一個貼進下面的引號裡即可。
   換金鑰只要改這個檔案、重新整理網頁，不用動其他程式碼。
   ========================================================== */

export const CONFIG = {
  // Firebase（見申請帳號步驟清單 2-4，一整段 firebaseConfig 裡的 6 個值）
  firebaseApiKey: "",
  firebaseAuthDomain: "",
  firebaseProjectId: "",
  firebaseStorageBucket: "",
  firebaseMessagingSenderId: "",
  firebaseAppId: "",

  // Cloudinary（見申請帳號步驟清單 3）
  cloudinaryCloudName: "",
  cloudinaryUploadPreset: "",

  // OpenWeatherMap（見申請帳號步驟清單 4）
  openWeatherApiKey: "",
};
